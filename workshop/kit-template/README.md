# {{BRAND}} — static kit

Generated by Workshop ({{DATE}}, vertical: {{VERTICAL}}, aesthetic: {{AESTHETIC}}).

Static HTML/CSS/JS only. No build step. Open `index.html` directly or serve with `python -m http.server`.

## Files

- `index.html`, `services.html`, `contacts.html` — pages
- `assets/css/style.css` — single stylesheet
- `assets/js/main.js` — single script
- `screenshots/` — preview captures (desktop 1440×900, mobile 390×844)
- `brief.md`, `audit.md` — synthesis notes from the run

## Telemetry placeholders

Replace `GA4_PLACEHOLDER` and `MS_CLARITY_PLACEHOLDER` in the page heads when deploying.

## Tier

See `audit.md` for suggested tier (1/2/3) based on self-audit quality.
